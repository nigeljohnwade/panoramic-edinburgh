type Resource
    @model
    @auth(rules: [
        {allow: owner},
		{allow: private, operations: [read]}
	])
{
    id: ID!
    type: ResourceType!
    title: String
}

enum ResourceType{
    PANORAMA
    SITE
    TOUR
    JOURNEY
}